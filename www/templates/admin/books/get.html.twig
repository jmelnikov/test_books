{% extends 'base.html.twig' %}

{% block title %}Добавление файла с книгами{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-md-center">
        <div class="col-md-6">
            <div class="card mb-3">
                <div class="row g-0">
                    <div class="col-md-3">
                        <img src="/images/{{ book.thumbnailUrl }}" class="img-fluid rounded-start" alt="{{ book.title }}">
                    </div>
                    <div class="col-md-9">
                        <div class="card-body">
                            <h5 class="card-title">{{ book.title }}</h5>
                            <p class="card-text">
                                <small class="text-body-secondary">
                                    {% for author in book.author %}
                                        {{ author.name }}{% if author != book.author|last %}, {% endif %}
                                    {% endfor %}
                                </small>
                            </p>
                            <p class="card-text">
                                <small class="text-body-secondary">
                                    Категории:
                                    {% for category in book.category %}
                                        {{ category.name }}{% if category != book.category|last %}, {% endif %}
                                    {% endfor %}
                                </small>
                            </p>
                            <p class="card-text">
                                <small class="text-body-secondary">
                                    Дата публикации: {{ book.publishDate|date('d.m.Y') }}<br/>
                                    ISBN: {{ book.isbn }}<br/>
                                    Количество страниц: {{ book.pageCount }}
                                </small>
                            </p>
                            <p class="card-text">{{ book.shortDescription }}</p>
                            <p class="card-text">{{ book.longDescription }}</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
